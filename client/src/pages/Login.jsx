import {useState} from 'react';import {useNavigate,Link} from 'react-router-dom';import {ShieldAlert} from 'lucide-react';import {useAuth} from '../context/AuthContext';
export default function Login(){const [email,setEmail]=useState('patient1@lifeconnect.demo'),[password,setPassword]=useState('Demo@12345'),[error,setError]=useState('');const {login,hospitalPath}=useAuth();const nav=useNavigate();return <div className="mx-auto mt-12 max-w-md rounded-3xl border bg-white p-8 shadow-sm"><ShieldAlert className="mx-auto text-red-600" size={42}/><h1 className="mt-4 text-center text-3xl font-black">Welcome back</h1><p className="mt-2 text-center text-slate-500">Emergency care coordination, one connection away.</p><form className="mt-8 space-y-4" onSubmit={async e=>{e.preventDefault();setError('');try{const u=await login(email,password);nav(u.role==='hospital'?await hospitalPath():u.role==='patient'?'/patient':`/${u.role}`)}catch(err){setError(err.response?.data?.message||'Login failed')}}}><input className="w-full rounded-xl border px-4 py-3" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="w-full rounded-xl border px-4 py-3" placeholder="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<div className="rounded-xl bg-red-50 p-3 text-red-700">{error}</div>}<button className="w-full rounded-xl bg-slate-900 px-4 py-3 font-bold text-white">Login</button></form><p className="mt-6 text-center text-sm">New patient? <Link className="font-bold text-red-600" to="/register">Create account</Link></p></div>}
