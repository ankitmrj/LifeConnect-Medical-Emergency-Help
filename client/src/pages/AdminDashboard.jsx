import {useEffect,useState} from 'react';import {api} from '../services/api';import {BarChart,Bar,XAxis,YAxis,Tooltip,ResponsiveContainer} from 'recharts';import StatCard from '../components/StatCard';import {Users,Hospital,Ambulance,Activity} from 'lucide-react';
export default function AdminDashboard(){const [s,setS]=useState(null),[h,setH]=useState([]);const load=()=>Promise.all([api.get('/admin/stats'),api.get('/admin/hospitals')]).then(([a,b])=>{setS(a.data.data);setH(b.data.data.hospitals)});useEffect(load,[]);return <div className="space-y-6">{s&&<><div><h1 className="text-3xl font-black">Admin control center</h1><p className="text-slate-500">Platform overview and verification</p></div><div className="grid gap-4 md:grid-cols-4"><StatCard label="Users" value={s.users} icon={Users}/><StatCard label="Hospitals" value={s.hospitals} icon={Hospital}/><StatCard label="Ambulances" value={s.ambulances} icon={Ambulance}/><StatCard label="Active emergencies" value={s.activeEmergencies} icon={Activity}/></div><div className="rounded-3xl border bg-white p-6 shadow-sm"><h2 className="font-black">Platform snapshot</h2><div className="mt-5 h-64"><ResponsiveContainer width="100%" height="100%"><BarChart data={[{name:'Patients',value:s.patients},{name:'Hospitals',value:s.hospitals},{name:'Ambulances',value:s.ambulances},{name:'Active',value:s.activeEmergencies},{name:'Completed',value:s.completedEmergencies}]}><XAxis dataKey="name"/><YAxis/><Tooltip/><Bar dataKey="value"/></BarChart></ResponsiveContainer></div></div><section className="rounded-3xl border bg-white p-6 shadow-sm"><h2 className="text-xl font-black">Hospital verification</h2><div className="mt-4 space-y-3">{h.filter(x=>!x.isVerified).map(x=><div key={x._id} className="flex justify-between rounded-xl border p-4"><span>{x.name}</span><button onClick={()=>api.patch(`/admin/hospitals/${x._id}/verify`).then(load)} className="rounded-lg bg-emerald-600 px-3 py-2 text-sm font-bold text-white">Verify</button></div>)}{!h.some(x=>!x.isVerified)&&<div className="text-slate-500">No pending verifications.</div>}</div></section></>}</div>}
