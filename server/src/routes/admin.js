import { Router } from 'express'; import { authenticateUser,authorizeRoles } from '../middleware/auth.js'; import {asyncHandler} from '../utils/errors.js'; import * as c from '../controllers/adminController.js';
const r=Router(); r.use(authenticateUser,authorizeRoles('admin')); r.get('/stats',asyncHandler(c.stats)); r.get('/users',asyncHandler(c.users)); r.get('/hospitals',asyncHandler(c.hospitals)); r.patch('/hospitals/:id/verify',asyncHandler(c.verifyHospital)); r.patch('/users/:id/disable',asyncHandler(c.disableUser)); r.get('/emergencies',asyncHandler(c.emergencies)); r.get('/ambulances',asyncHandler(c.ambulances)); export default r;
