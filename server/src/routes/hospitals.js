import { Router } from 'express'; import { authenticateUser,authorizeRoles } from '../middleware/auth.js'; import { asyncHandler } from '../utils/errors.js'; import * as c from '../controllers/hospitalController.js';
const r=Router(); r.get('/',asyncHandler(c.list)); r.get('/nearby',asyncHandler(c.nearby)); r.get('/dashboard/me',authenticateUser,authorizeRoles('hospital'),asyncHandler(c.dashboard)); r.post('/',authenticateUser,authorizeRoles('hospital','admin'),asyncHandler(c.create)); r.put('/:id/resources',authenticateUser,authorizeRoles('hospital','admin'),asyncHandler(c.resources)); r.get('/:id',asyncHandler(c.details)); export default r;
